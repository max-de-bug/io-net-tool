// "use server";
// import { getServerSession } from "next-auth/next";
// import { authConfig } from "../../../lib/auth";
// import * as Api from "../../app/api/index";

// export const serverDataDashboard = async ({ userId }) => {
//   const serverData = await Api.server.getServer(userId);
//   console.log("serverDataDashboard", serverData);
//   return { serverData };
// };

// export default serverDataDashboard;
